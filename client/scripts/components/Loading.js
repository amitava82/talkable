import React from 'react';
import CircularProgress from 'material-ui/CircularProgress';

export default function () {
    return (
        <CircularProgress style={{display: 'flex', alignItems: 'center', margin: 'auto', height: '100vh'}} />
    );
}